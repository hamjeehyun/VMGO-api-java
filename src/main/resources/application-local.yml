# 로컬 개발 설정
application:
  workspace: LOCAL
  port: 8006

spring:
  config:
    activate:
      on-profile: local

database:
  url: jdbc:mariadb://
  username:
  password:

hikari:
  connection-timeout: 180
  maximum-pool-size: 2000
  max-lifetime: 3000

logging:
  lovel: DEBUG

firebase:
  path: ""
  key: ""


